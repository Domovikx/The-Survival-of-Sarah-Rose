"""voicekit — ядро TSSR voice pipeline.

ПРАВИЛО ИНКАПСУЛЯЦИИ (для агентов): импортировать только НАШИ модули.
Код извне репозитория (W40K tools, чужие скрипты) НЕ вызывается и НЕ
правится — нужная функция КОПИРУЕТСЯ сюда и используется своя.

Единственный источник правды по раскладке проекта (paths), загрузке
каталога/голосов/каста (catalog), контракту каста (contract), безопасным
файловым операциям (fs), окружению TTS (tts_env) и модельным директориям
CosyVoice3 с атомарной правкой yaml (model_dir).
"""

from . import paths, catalog, contract, fs, tts_env, model_dir  # noqa: F401