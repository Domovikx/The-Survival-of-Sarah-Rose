---
description: "Перевести арку/файлы Ren'Py: нормализовать пути и запустить параллельный перевод через task(general)"
---

Загрузи скил orchestrator.

Нормализуй пути: $ARGUMENTS

Если $ARGUMENTS не содержит полного пути — ищи файлы в `game/tl/ru/`.
Можно передать как имя арки (`PrisonArc`), так и конкретные файлы.

Передай нормализованный список файлов скилу orchestrator.
Верни отчёт пользователю.
