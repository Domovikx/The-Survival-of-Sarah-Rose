---
name: tssr-voice-status
description: Статус и прогноз генерации озвучки TSSR. Используй, когда пользователь спрашивает «статус генерации», «как идёт озвучка», «сколько готово», «прогноз/когда закончится», «какие арки готовы» или просит проверить здоровье генерации (воркеры, сироты). ЗАПРЕЩЕНО считать вручную — только этот тул.
---

# Статус генерации озвучки TSSR

ЕДИНЫЙ тул для статуса/прогноза — не считай вручную (find/wc/grep по ai_voice — нет, это дублирование и ошибки).

## Когда вызывать

Пользователь спрашивает:
- «статус?», «как идёт генерация?», «сколько готово?», «прогноз?», «когда закончится?»
- «какие арки готовы?», «что сейчас генерится?»
- «проверь здоровье» (воркеры, tmp-сироты)

## Команда

```bash
python tools/voice_status.py                    # полный отчёт + замер (~70 с)
python tools/voice_status.py --fast             # мгновенно, без замера
python tools/voice_status.py --arc Other        # точечно: по конкретной арке
                                                #   (RU/EN, по голосам, размер)
python tools/voice_status.py --char Sarah       # точечно: по персонажу
                                                #   (план фраз, сделано, по аркам)
python tools/voice_status.py --detail           # подробный: арки×проценты×размер,
                                                #   топ голосов, вокеры, сироты
```

## Что выводит тул

- План: 134 534 фразы (67 267 озвучиваемых × RU+EN)
- RU/EN: сколько wav, %, размер
- Арки: сделано/всего, завершённые помечены ✓
- Вокеры: число voice_batch-процессов (python/2), активная арка (из логов gen_all_{ru,en}.log)
- Здоровье: tmp-сироты loudnorm (*.ln.tmp.wav — их удалять нельзя слепо: финалы валидны, сироты — мусор от kill)
- Скорость (замер прироста 60 с) → фраз/час, остаток, прогноз в часах/днях

## Как интерпретировать

- **Активная арка**: последняя START-метка в `output/voice/gen_all_{lang}.log` без DONE после неё
- **Скорость** волатильна (CPU делится с игрой/системой) — прогноз ориентировочный
- **tmp-сироты** появляются при kill/ребуте в окне ffmpeg→os.replace; финальные wav при этом НЕ страдают (остаются валидной версией) — это выяснено 2026-09-05, уровень финалов -16 LUFS подтверждён volumedetect
- Ускорение: добавить воркеры (до 4), но CPU забивается; или резать по аркам (`--arc`)

## Контекст

Очереди: `tools/voice_queue.sh {ru|en}` (bash, resumable, низкий приоритет, переживают ребуты).
Логи: `output/voice/gen_all_{ru,en}.log`. Рестарт после ребута — см. AGENTS.md.